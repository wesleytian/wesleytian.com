import React from "react";

const Divider = () => {
  const dividerStyle = {
    borderTop: "1px solid #e0e0e0",
    margin: "1rem 0"
  };

  return <hr style={dividerStyle} />;
};

export default Divider;
